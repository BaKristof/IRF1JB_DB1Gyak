Create Table gyarto (
adoszam int,
nev char(30),
telephely text(100),
Primary Key adoszam
);
Create Table termek(
tkod int,
nev char(50),
ear number(<0),
gyarto int,
primary key tkod,
Foreign key(gyarto) references gyarto(adoszam)
);